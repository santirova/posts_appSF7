<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="{{ path('posts_home') }}">
            MyWeb
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_logout') }}">Logout <i class="bi bi-box-arrow-right"></i></a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('my_user') }}">Profile <i class="bi bi-person"></i></a>
                    </li>
                {% else %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_user_new') }}">Register <i class="bi bi-person-plus"></i></a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_login') }}">Login <i class="bi bi-box-arrow-in-right"></i></a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>